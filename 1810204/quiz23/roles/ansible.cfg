[defaults]

inventory = ./inventory
remote_user = matienzo-1810204
private_key_file = ./id_rsa

#enable below if needed sudo
[privilege_escalation]
become = True
become_method = sudo
become_user = root
become_ask_pass = False
