---
# tasks file for roles/install_grafana_ubuntu

  - name: Adding the apt key
    apt_key:
      url: "https://packages.grafana.com/gpg.key"
      state: present

  - name: Adding the repository
    apt_repository:
      repo: "deb https://packages.grafana.com/oss/deb stable main"
      state: present
      filename: grafana.list

  - name: Installing grafana
    apt:
      name: grafana
      state: latest
      update_cache: yes

  - name: Starting grafana
    service:
      name: grafana-server
      state: started
      enabled: yes

  - name: Stopping ufw
    service:
      name: ufw
      state: stopped
      enabled: no
